<Page
    x:Class="CurrencyConverter.ListOfCurrencyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CurrencyConverter.Data"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="White">

    <Grid>
        <ListBox 
            x:Name="valuteList" 
            SelectionChanged="CurrencyList_SelectionChanged" >
            <ListBox.ItemTemplate>
                <DataTemplate x:DataType="local:Currency">
                    <StackPanel Orientation="Horizontal" Margin="10">                        
                        <TextBlock Text="{x:Bind CharCode}" VerticalAlignment="Center" />
                        <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center" Margin="10 0 0 0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
